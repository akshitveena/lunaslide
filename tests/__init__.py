"""Test package marker.

Present so the suite is discoverable with the standard library alone:

    python3 -m unittest discover -s tests -t . -v

pytest works too, but should not be required to run the tests.
"""
